// Generated by `yarn new:mutationMock`
import { server } from '../../server'
import { createContactMutation } from '../createContact';
import { Options } from './type'

export const setCreateContactMutation = (options: Options) => {
  server.use(createContactMutation(options))
}
